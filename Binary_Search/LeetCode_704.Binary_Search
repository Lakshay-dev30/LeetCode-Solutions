//Brute Force

boolean flag = false;
for(int i=0;i<n;i++){
		if(arr[i] == target){
			flag = true;
		}
}
if(flag == true) sout("Element Find");
else sout("Element not Present");

//T.C = O(n)
//S.C = O(1)
